# G5 real-capability vertical-slice checkpoint

Status: `in_progress`

Gate passed: `false`

Release ready: `false`

This checkpoint proves 164 formal V4 read success paths and 106 write success
paths through the real Odoo 19 bridge. It does not claim that all planned
capabilities or G5 are complete. The registry is now a 289-ID baseline, after
the eight-command sales-invoice/stock-transfer write batch, the nine-command
specialist/localized report-export extension, the ten-command
native financial-report export batch, the nine-command fiscal-
position/journal-group/localization-readiness batch,
the nine-command accounting follow-up batch, the twelve-command sales/purchase
order-write batch, the historical 236-ID
order-document read batch, the historical
228-ID operational-inventory read batch, the historical 218-ID
account-return/journal-analysis read batch, the 210-ID
management-reporting/period-context read batch, the
historical 202-ID checkpoint, the 190-ID partner-master-data, 179-ID
analytic/budget-write, 170-ID
payment/bank reconciliation,
160-ID document-lifecycle,
152-ID analytic/budget, 141-ID
payment/reconciliation-object, 133-ID accounting-configuration, 125-ID
reference-object, and historical 113-ID core-object read checkpoints, with 272
handlers: 165 reads and 107 writes. One hundred sixty-four reads and 106 write
paths have dual-database success-path verification; 17 capabilities remain disabled/planned
(8 reads and 9 writes), and 549 versioned JSON Schema documents are retained.
Current statuses are exactly 251 `unconfigured`, 21 `degraded`, and 17
`disabled`.
`asset.validate` is implemented but its current-server success path is blocked;
the normalized error and transaction rollback are verified instead.
The 289-ID baseline is not a proven final capability count. The historical
125-ID checkpoint was only the `102 + 11 + 12` delivery lineage, not a target or
sizing basis.

## Implemented vertical slice

- The latest batch adds `sale.order.invoice.create`, `stock.transfer.create`,
  `stock.transfer.confirm`, `stock.transfer.assign`,
  `stock.transfer.quantities.set`, `stock.transfer.validate`,
  `stock.transfer.unreserve`, and `stock.transfer.cancel`. The shared guarded
  smoke passed in both isolated aliases, including immediate replay, a real
  backorder, and rollback; Odoo 19 UoM compatibility uses the native
  `_has_common_reference()` API.
- The preceding batch adds `report.journal.export`, `report.asset.export`,
  `report.deferred_expense.export`, `report.deferred_revenue.export`,
  `report.multicurrency_revaluation.export`,
  `report.china.balance_sheet.export`, `report.china.profit_and_loss.export`,
  `report.china.cash_flow.export`, and `report.singapore.gst.export`. All nine
  reuse the fixed native report-export path; localized exports enforce the
  company fiscal-country/chart boundary.
- The preceding batch adds `report.trial_balance.export`,
  `report.balance_sheet.export`, `report.profit_and_loss.export`,
  `report.cash_flow.export`, `report.tax.export`,
  `report.general_ledger.export`, `report.partner_ledger.export`,
  `report.aged_receivable.export`, `report.aged_payable.export`, and
  `report.executive_summary.export`. All ten use the fixed native
  `account.report.fixed_export` action for strict PDF/XLSX output.
- The preceding batch adds `fiscal_position.create`, `fiscal_position.update`,
  `fiscal_position.account_mappings.replace`, `fiscal_position.archive`,
  `fiscal_position.restore`, `journal.group.create`, `journal.group.update`,
  `localization.china.configuration.inspect`, and
  `localization.singapore.configuration.inspect`.
- The preceding accounting follow-up batch adds `purchase.order.bill.create`,
  `purchase_bill.match`, `purchase_bill.lines.unmatch`, `payment_term.create`,
  `payment_term.update`, `payment_term.lines.replace`, `payment_term.archive`,
  `payment_term.restore`, and `period.accrual.generate`.
- The latest sales/purchase order-write batch adds `sale.order.create`,
  `sale.order.update_draft`, `sale.order.lines.replace`, `sale.order.confirm`,
  `sale.order.cancel`, `sale.order.reset_to_draft`, `purchase.order.create`,
  `purchase.order.update_draft`, `purchase.order.lines.replace`,
  `purchase.order.confirm`, `purchase.order.cancel`, and
  `purchase.order.reset_to_draft`. All twelve use the shared fixed core-write
  contract and one allowlisted company/user-scoped ORM runtime.
- The preceding sales/purchase order-document read batch adds `sale.order.search`,
  `sale.order.get`, `sale.order.line.search`, `sale.order.analysis.summary`,
  `purchase.order.search`, `purchase.order.get`, `purchase.order.line.search`,
  and `purchase.order.analysis.summary`. All eight use fixed contracts, one
  allowlisted bridge action, and company/user-scoped native Odoo models.
- The preceding operational-inventory read batch adds `product.category.list`,
  `warehouse.list`, `stock.location.list`, `stock.operation_type.list`,
  `stock.route.list`, `stock.transfer.search`, `stock.transfer.get`,
  `stock.move.search`, `inventory.on_hand.summary`, and
  `inventory.availability.inspect`. All ten use fixed validators, two
  allowlisted bridge actions, company/user-scoped native Odoo models, and one
  shared rollback-only smoke in both dedicated database aliases as uid 5.
- The preceding account-return/journal-analysis read batch adds
  `account.return.search`, `account.return.get`, `account.return.summary`,
  `account.return.type.list`, `account.return.check.list`,
  `account.return.check.get`, `journal.accounting_date.resolve`, and
  `journal_item.analysis.summary`. All eight use fixed validators and native
  company/user-scoped Odoo routes and passed one shared rollback-only smoke in
  both dedicated database aliases as uid 5.
- The preceding management-reporting/period-context read batch adds
  `report.customer_statement`, `report.followup`,
  `invoice.analysis.search`, `invoice.analysis.summary`,
  `company.lock_dates.inspect`, `company.fiscal_year.resolve`,
  `fiscal_year.search`, and `fiscal_year.get`. The report commands use fixed
  official report handlers or the native invoice-analysis model; fiscal-year
  resolution uses `res.company.compute_fiscalyear_dates` and the other period
  commands use fixed company-scoped models. All eight passed one shared
  rollback-only smoke in both dedicated database aliases as uid 5.
- `account.account.list`, `company.accounting_context.list`, `journal.list`,
  `tax.list`, `payment_term.list`, `currency.list`, `journal_entry.search`, and
  `journal_entry.get`, `report.trial_balance`, `report.balance_sheet`, and
  `report.profit_and_loss`, `report.cash_flow`, `report.tax`, and
  `user.accounting_access.inspect`, `company.accounting_configuration.inspect`,
  `diagnostic.accounting_environment.inspect`, `partner.accounting.search`,
  `invoice.search`, `invoice.get`, `invoice.payment_status.inspect`,
  `receivable.open_items.list`, `payable.open_items.list`, `payment.search`,
  `payment.get`, and `reconciliation.candidates.list` are
  dispatched through the allowlisted V4 control CLI,
  a single-document stdin/stdout JSON subprocess protocol, the Odoo 19 Python
  runtime, Registry/Environment, and their fixed real ORM models:
  `account.account`, `account.journal`, `account.tax`,
  `account.payment.term`/`account.payment.term.line`, `res.currency`,
  `account.move`, `account.move.line`, `res.company`, `res.users`,
  `ir.module.module`, `res.partner`, `account.partial.reconcile`, `account.payment`,
  `account.payment.method`, `account.payment.method.line`, `product.product`, and
  `account.reconcile.model`, plus the fixed Odoo financial-report definitions
  and `account.report` handlers.
- `currency.rate.list` is also implemented and live-verified through the fixed Base ORM
  data models `res.company`, `res.currency.rate`, and `res.currency`, with
  `res.users` used for the configured-user group gate, exact `date_from`,
  `date_to`, and `currency_id` filters, and a dedicated schema.
- The historical extended-read batch adds `currency.convert`,
  `validation.journal_entry.check`, six typed accounting reports,
  `bank.transaction.list`, and `product.accounting_profile.get`. The first nine
  passed the shared dual-database live success smoke. Product-profile routing
  and not-found behavior passed, while a live success mapping remains pending
  because the isolated databases contain no products.
- The inventory-accounting read batch adds `cogs.entries.list`,
  `inventory.accounting_entries.list`, `report.inventory_valuation`,
  `purchase_bill.matching.inspect`, and `sale_invoice.stock_link.inspect`.
  All five have fixed request/response schemas, allowlisted bridge/runtime
  routing, CLI handlers, and shared dual-database read-only smoke evidence.
- The fixed-asset batch adds `asset.search`, `asset.get`,
  `asset.depreciation_schedule.get`, and `report.asset`. All four have dedicated
  schemas, fixed company-scoped actions, ordinary-accountant ACL checks, and
  shared dual-isolated-database success evidence.
- The historical remaining-read batch adds seven fixed official accounting reports,
  native fiscal-position resolution, and native journal hash-integrity
  inspection. All nine passed one shared 18-case smoke across both dedicated
  database aliases. CN and SG reports are explicitly bound to the matching
  fiscal country and chart template.
- The historical core-object read batch adds `account.account.get`, `journal.get`,
  `tax.get`, `payment_term.get`, `currency.get`, `partner.accounting.get`,
  `bank.transaction.get`, `journal_item.search`, `journal_item.get`,
  `payment.method.list`, and `reconciliation.model.list`, and enables
  `report.bank_reconciliation` with an explicit company-scoped bank
  `journal_id`. All twelve passed guarded read-only live runs in both dedicated
  aliases.
- The reference-object read batch adds `product.search`, `product.get`,
  `analytic.plan.list`, `analytic.plan.get`, `analytic.account.search`,
  `analytic.account.get`, `fiscal_position.search`, `fiscal_position.get`,
  `account.tag.list`, `account.tag.get`, `tax.group.list`, and `tax.group.get`.
  All twelve passed one guarded transactional live smoke across both dedicated
  aliases, followed by whole-transaction rollback and a fresh-cursor
  zero-residue check.
- The accounting-configuration read batch adds `payment.method.get`,
  `reconciliation.model.get`, `cash_rounding.list`, `cash_rounding.get`,
  `journal.group.list`, `journal.group.get`, `incoterm.list`, and
  `incoterm.get`. All eight passed one guarded transactional live smoke across
  both dedicated aliases, followed by whole-transaction rollback and a
  fresh-cursor zero-residue check.
- The historical payment/reconciliation object read batch adds
  `partner.bank_account.search`, `partner.bank_account.get`,
  `bank.statement.search`, `bank.statement.get`, `reconciliation.partial.list`,
  `reconciliation.partial.get`, `reconciliation.full.list`, and
  `reconciliation.full.get`. All eight passed one guarded transactional smoke
  in both dedicated aliases with fresh-cursor rollback verification.
- The analytic/budget read batch adds `analytic.line.search`,
  `analytic.line.get`, `analytic.distribution_model.list`,
  `analytic.distribution_model.get`, `analytic.applicability.list`,
  `analytic.applicability.get`, `budget.search`, `budget.get`,
  `budget.line.list`, `budget.line.get`, and `report.budget`. All eleven passed
  one guarded transactional smoke in both dedicated aliases. The dedicated
  report path preserves overlapping official budget rows with repeated text
  row keys by using a fixed composite cursor.
- The payment/bank reconciliation batch adds
  `bank.transaction.search`, `bank.transaction.reconciliation.get`,
  `bank.transaction.match_candidates.list`, `payment.create`,
  `payment.update_draft`, `payment.reset_to_draft`, `bank.transaction.update`,
  `bank.transaction.match`, `bank.transaction.unmatch`, and
  `reconciliation.write_off`. The three reads and seven writes use fixed
  contracts and allowlisted ORM routes; all ten passed one shared guarded smoke
  across both dedicated aliases with fresh-cursor rollback verification.
- The analytic/budget write batch adds `analytic.account.create`,
  `analytic.account.update`, `budget.create`, `budget.update_draft`,
  `budget.lines.replace`, `budget.confirm`, `budget.reset_to_draft`,
  `budget.cancel`, and `budget.mark_done`. All nine use fixed contracts and
  allowlisted ORM routes and passed one shared guarded dual-database smoke with
  first execution, immediate replay, rollback, and fresh-cursor verification.
- The current accounting-configuration write batch adds
  `account.account.create`, `account.account.update`,
  `account.account.archive`, `account.account.restore`, `journal.create`,
  `journal.update`, `journal.archive`, `journal.restore`, `tax.create`,
  `tax.update`, `tax.archive`, and `tax.restore`. All twelve passed one shared
  guarded dual-database transactional smoke with first execution, immediate
  replay, whole-transaction rollback, and fresh-cursor absence verification.
- The preceding partner master-data batch adds `partner.search`, `partner.get`,
  `partner.create`, `partner.update`, `partner.archive`, `partner.restore`,
  `partner.accounting.update`, `partner.bank_account.create`,
  `partner.bank_account.update`, `partner.bank_account.archive`, and
  `partner.bank_account.restore`. All eleven use fixed contracts and allowlisted
  `res.partner`/`res.partner.bank` ORM routes and passed one shared guarded
  dual-database smoke with first execution, immediate replay, rollback, and
  fresh-cursor residue verification.
- The historical extended-write batch adds three native asset lifecycle actions,
  deferred expense/revenue and multicurrency revaluation entry generation,
  automatic reconciliation, and generic/China period transfers. All nine ran
  through the public write contract, including immediate replay, in both
  dedicated aliases before full transaction rollback and fresh-cursor absence
  verification.
- Runtime targets are resolved from an explicit database/company/user
  allowlist. The business Environment uses the configured non-superuser and
  company scope; V4 does not introduce a sudo read path. Odoo's native journal
  integrity method internally uses `sudo` only to compute complete hash chains
  before returning its aggregate integrity result.
- Ordinary read cursors execute `SET TRANSACTION READ ONLY`. Typed Odoo reports
  use rollback-only transactions because their official handlers may create
  temporary tables; every path rolls back and closes. No general model/method
  dispatcher or direct business SQL is exposed.
- Company visibility, module state, ACL, page data, and the reported Odoo user
  ID are obtained by one allowlisted action in the same read-only transaction.
- An inactive requested Odoo language now returns the structured
  `language_unavailable` error instead of an opaque runtime failure or an
  implicit fallback.

## Core write batch

- The first fixed write batch implements `customer_invoice.create`,
  `vendor_bill.create`, `invoice.post`, `journal_entry.create`,
  `journal_entry.post`, `journal_entry.reverse`,
  `receivable.payment.register`, `payable.payment.register`,
  `reconciliation.apply`, and `payment.cancel` through `write run`. Each call
  requires the exact capability confirmation and an idempotency key; no generic
  model or method dispatcher is exposed.
- The second fixed write batch adds `customer_credit_note.create`,
  `vendor_refund.create`, `payment.post`, `reconciliation.undo`, and
  `bank.transaction.record`. Refunds use Odoo's whole-document reversal wizard,
  legacy two-line reconciliation undo removes only the exact isolated partial
  pair, and bank recording returns the auto-posted underlying journal entry.
  The later accounting-depth batch adds invoice-targeted undo that removes only
  the requested partial and returns the remaining reconciliation graph.
  `payment.post`
  uses a guarded test-only uid 5 draft-payment prerequisite in the two isolated
  databases; it does not expose a generic payment-creation path.
- The bridge executes as the configured non-superuser, preserves Odoo ACL and
  company scope, and commits only the fixed write action. Create operations use
  Odoo-persisted request markers; post, reversal, payment, reconciliation, and
  cancellation replay from the resulting Odoo business state.
- Guarded live smoke log ID `7ed80e35-71e9-43de-a16f-1046d8b4e453` ran the
  complete batch against `v4-dev` (`odoo_cli_v4_dev`) and `v4-e2e`
  (`odoo_cli_v4_e2e`). Every command succeeded once and then returned the same
  result as an immediate idempotent replay; pytest recorded `1 passed in
  412.03s`. The retained server log is
  `/opt/odoo-accounting-cli-v4/.tooling/core-write-smoke-7ed80e35-71e9-43de-a16f-1046d8b4e453.log`.
- Guarded live smoke log ID `a6c34c19-972d-4a83-a3ad-b97925db95d5`
  reran both write batches against the same two dedicated aliases. All fifteen
  commands completed their required first execution and immediate replay;
  pytest recorded `1 passed in 667.53s`. The retained server log is
  `/opt/odoo-accounting-cli-v4/.tooling/core-write-smoke-a6c34c19-972d-4a83-a3ad-b97925db95d5.log`.
- The first second-batch attempt reached a successfully created uid 5 draft
  payment but the test rejected an expected Odoo field-precomputation warning
  on stderr. Only that over-strict test assertion was removed; process exit,
  single-JSON output, user/company/database, draft state, and subsequent CLI
  verification remain enforced. Its already committed records remain only in
  `odoo_cli_v4_dev` as synthetic test data; no business database was touched.
- A rollback-only diagnostic identified the initial reversal failure as Odoo
  19's required `account.move.reversal.journal_id` being absent from direct ORM
  wizard creation. Passing the already scoped source journal is the only runtime
  correction; the rollback diagnostic then completed reversal and marker write.
  The first-batch local unit suite recorded `1444 passed in 348.84s`, and its
  focused runtime suite passed `21` tests both locally and on the server. After
  the second batch, the complete local unit suite recorded `1511 passed in
  634.08s`; the combined write-contract/runtime/CLI/registry suite recorded
  `164 passed` locally and `164 passed in 61.98s` on the server.
- `currency.rate.record` remains disabled rather than being falsely counted as
  operational. A rollback-only real Odoo probe confirmed that the current
  ordinary-accountant uid 5 lacks the manager-only `res.currency.rate` create
  ACL; no `sudo` or user-group change was introduced.

## Fixed-asset batch

- `asset.create` and `asset.validate` extend the fixed `write run` path without
  introducing a generic model or method dispatcher. Both require exact
  capability confirmation, company/user scope, Odoo ACL checks, and an
  idempotency key. Create accepts a closed 13-field draft contract and uses a
  deterministic visible name suffix. Odoo has no native unique asset reference,
  so this implementation does not claim concurrent exactly-once creation.
- `asset.search`, `asset.get`, and `asset.depreciation_schedule.get` use one
  company-scoped read action across the fixed asset, journal, account, move,
  company, and currency models. `report.asset` calls Odoo's official asset
  report and normalizes its real twelve-column layout, including the blank
  `balance` title as `Book Value`.
- The full local regression recorded `1657 passed, 106 skipped in 358.38s`;
  every skip was an opt-in live test without local Odoo configuration. After
  synchronization, the server-focused suite recorded `272 passed, 1 skipped in
  116.99s`. Its retained log SHA-256 is
  `c00c594b4c52e0a5a47fe8a8cec43287c5b08d67e47779db00fa9acb145ea594`.
- The guarded shared server smoke recorded `1 passed in 118.49s` against only
  `odoo_cli_v4_dev` and `odoo_cli_v4_e2e`. It verified successful create plus
  immediate replay, search, detail, empty draft schedule, and official report
  calls in both databases. The retained log and SHA-256 are
  `/opt/odoo-accounting-cli-v4/.tooling/asset-batch-live-1c2cc43ca32745af8333db1e86cc1e50.log`
  and `2e37faecdb553a6d34663ff72f8c7dd4f5384bd3dd4aad72ce041d2b4d26dbed`.
- The same test intentionally expects `asset.validate` to fail on this server;
  its passing pytest result is rollback evidence, not a validation success.
  Odoo's native validation creates multiple depreciation moves, after which
  `/mnt/odoo/odoo19/custom/addons/exchange_currency_rate/models/account_move.py`
  lines 47-51 raises `Expected singleton`. The CLI returns a sanitized
  `odoo_write_error`; follow-up reads prove state `draft` and no depreciation
  moves in both databases. The third-party add-on was neither changed nor
  bypassed.
- Run `4a2e4481-ba34-45b2-9c89-9c59a484a85d` intentionally retains one
  synthetic draft asset in each isolation database: dev ID `25` and e2e ID
  `13`, both company 1/CNY with original and book value `1200`. The current live
  fixture proves an empty draft schedule and schema-valid report invocation;
  it does not prove a non-empty depreciation schedule or report body.
- Main Odoo PID `3034807` and its `Mon Aug 24 10:24:18 2026` start time were
  unchanged after the batch. No service restart occurred.

## Inventory-accounting read batch

- The five commands use fixed Odoo relationships rather than a general model
  dispatcher. COGS is limited to posted customer invoices/refunds;
  inventory-accounting entries require done stock moves with posted accounting
  moves; valuation normalizes Odoo's official stock-valuation report; purchase
  matching explicitly scopes its view by company; and sales stock links follow
  invoice lines to sale lines and completed stock moves.
- Review found and corrected four contract-critical edges: delivery-versus-
  return direction filtering, refund fallback only for a unique source product
  line, cancelled bills not being falsely required to appear in the unmatched
  queue, and explicit company-domain plus record-level revalidation. In
  particular, `purchase.bill.line.match` has no company record rule and is not
  trusted without the explicit company filter.
- The focused capability/schema/bridge/runtime/CLI/registry tests recorded
  `107 passed in 62.74s` locally and `107 passed in 78.45s` on the server.
  The post-batch full local unit regression recorded
  `1578 passed in 286.23s`.
  The shared ordinary-accountant smoke ran as uid 5/company 1 against both
  dedicated database aliases and recorded `2 passed in 89.71s`. Its retained
  server log is
  `/opt/odoo-accounting-cli-v4/.tooling/inventory-read-smoke-3e199cc1-10c8-4bfa-b230-0860b1f91bd1.log`.
- Odoo PID `3034807` was unchanged after the smoke; the service was not
  restarted. The real fixtures currently produce empty COGS entries, empty
  inventory accounting entries, no sales stock links, and zero inventory
  valuation, while purchase matching returns a non-empty unmatched queue.
  Consequently, non-empty inventory associations and a real cursor transition
  remain unverified by live fixtures and are not claimed here.

## Payment vertical slice

- `payment.search` and `payment.get` now have closed V1 schemas, allowlisted
  handlers, CLI wiring, and fixed read-only Odoo actions. Search uses a
  database/company/user/filter-bound descending `(date,id)` cursor and exact
  filters. Get keeps direct invoice links separate from explicitly traversed
  receivable/payable partial-reconciliation provenance instead of trusting
  Odoo's computed reconciliation convenience fields.
- The payment-focused contract, bridge, CLI, runtime, and registry matrix passed
  179 focused tests locally. The retained gate repeated the complete 860-test
  unit suite and ran four live database/company cases with zero failures,
  errors, skips, or disabled cases; the registry therefore records both
  integrations as implemented while runtime availability remains
  target-specific.
- The payment gate run ID is `13af3252-0846-4b04-9132-d6488e42f67e`. Its
  independent business-user, non-sudo ORM comparator made 28 formal CLI
  invocations across both physical databases and both configured companies:
  four search cases and three get cases per context. It matched every public
  field, all nine filters, found/cross-company/missing get behavior, same-root
  authorized reconciliation scope, linked-document company provenance, and
  Odoo `ilike` query delegation. All 24 selected table fingerprints, the exact
  18 candidate files, Git/config/database/service identity, and immutable
  source/environment snapshots remained unchanged; fresh-wheel
  `payment.search` and `payment.get` smokes passed. The retained summary,
  direct-comparison output, `SHA256SUMS` manifest, and wheel SHA-256 values are
  `fed0a238c0ab8022fa29d248e3ae4784dfc16ae0dc779d83eddb512eb79d9cf8`,
  `1ad2c4ae7893e5bd0dd80b7f5ebef5cb1f08823b3b969b4e005cd7e1a2b39abf`,
  `f025a6b964452d47c557bb8059b358dfdf196864a9623c0185cd15a597fea166`,
  and `6160149337a51db88ebc6ea99ce98b610e738676e415f1228e37a7a6b47b498b`.
- Fixture v1 has exactly one inbound/customer/in-process payment per company,
  so live evidence proves terminal pages but not a real cursor transition,
  outbound/supplier payments, other states, detached method lines, foreign
  currency, or multi-document reconciliation; those remain unit-tested or
  require a richer versioned fixture.

## Reconciliation-candidate vertical slice

- `reconciliation.candidates.list` implements the fixed `account_accountant`
  `Journal Items to reconcile` default read. It is company- and configured-user
  scoped, limits results to authorized reconcilable accounts and non-zero
  residual rows without a full reconciliation, delegates query matching to
  Odoo `ilike`, and uses a database/company/user/filter-bound descending
  `(date,id)` cursor.
- Retained gate run `3f941ac7-7561-4ea9-ada5-74aaeb071065` recorded
  `1047` passing unit tests and
  `4` passing live tests with zero
  failures, errors, skips, or disabled cases. Its independent business-user,
  non-sudo ORM comparator made
  `56` formal CLI invocations across
  both physical databases and both configured companies. It compared every
  public field and proved the default candidate boundary, exact filter and
  query results, pagination, and company isolation. Retained source and unit
  traces separately verified Odoo Domain delegation and exact related-row
  rereads. Candidate files, database fingerprints, runtime configuration,
  and service/source identities remained unchanged; the fresh-wheel smoke
  passed.
- The retained summary, direct-comparison output, `SHA256SUMS` manifest, and
  wheel SHA-256 values are
  `c7010051b0a793bacd37d93d1fad43b82d23548e641f73ae57380e0711673d7f`,
  `0442dac74a76b8c4eb43d3396db33d4f64fcff89658ff39f193801e22dc5060e`,
  `b8bb4f878130006a9fcf78e8239accba22f548a91689d0c6b99554e7e1e2a642`, and
  `b8bf240f221a2cbceb737b14cc3f11acce0633038a5d93f2e4fb0803d6934a21`.
- Fixture v1 has exactly three posted, same-currency default candidates per
  company (bank suspense, trade payable, and trade receivable), all with a
  partner and `reconciliation_model` equal to null. Live evidence therefore
  does not cover draft-state, cross-currency, null-partner, non-null historical
  reconciliation-model, or pages larger than this three-row fixture; those
  boundaries remain contract/unit proven only.

## Real verification

- For `account.account.list`, both dedicated synthetic environment aliases
  passed two cursor pages: 2 tests passed, zero skipped, and stderr was empty.
  Each response was one schema-valid `verified` JSON document, company scoped,
  strictly ordered by `(code, id)`, and free of page overlap.
- The four master-data capabilities passed 8 additional dual-environment live
  tests with two cursor pages each. Their fixed orders are
  `(sequence, type, code, id)` for journals, `(sequence, id)` for taxes and
  payment terms, and `(active DESC, code, id)` for currencies. The first three
  apply the documented company/shared-company scope; currencies are global
  records with a company-relative `is_company_currency` value.
- For `account.account.list`, a separately executed Odoo-side ORM query, under the same synthetic user,
  company, language, timezone, and an independently opened read-only
  transaction, matched the formal CLI page and reported user ID in both
  environments.
- Selected business-table fingerprints were identical before and after all
  current live checks in both databases.
- The preceding account-list composite-action private gate run ID is
  `369d178d191f4ad8892a8c0aecf7a1e3`. It recorded 86 passing tests, zero
  stderr, a successful direct-ORM comparison, and matching pre/post
  fingerprints for both databases.
- The private evidence manifest SHA-256 is
  `8d1ce9a6cd1ed51a1d217ea3fb4e4a0f0ca62a419ab123d1a59dc2155c7a055d`;
  it records the candidate-file manifest, exact test command, fingerprint
  algorithm and tables, runtime-configuration digest, and hashes of the
  retained verification programs and results.
- The current test output and direct-ORM comparison SHA-256 values are
  `99d1d3f304e5f742d0413074a1dfb066aedade9694eaab42ea131e6eb82a5ca5`
  and `4c0b921722b74d5f62d8de9d15812f6e2768b166a93beb20c23283be996ab093`;
  both stderr files have the empty-file digest. The dev and e2e fingerprint
  value-file SHA-256 values are
  `617391097b9a8313c6db8d6878c83b9f4758a2100a51ab67878cafdad4d03426`
  and `aa333a552b8f36cc1f9d47d598b4913b6a12d320545eca839ef9719ac784a0e4`.
- The current four-capability master-data gate run ID is
  `c7e0d992-6f9b-48af-8239-9f8d4a374719`. It recorded 176 passing tests in
  71.98 seconds, zero stderr, stable candidate files and runtime configuration,
  and unchanged pre/post fingerprints across 15 selected tables in both
  dedicated databases.
- The current gate summary JSON SHA-256 is
  `ca9e8402104d09c00d1c204e99be5acda94236d89d1dcc7364ec8155b99d8939`.
- Independent direct-ORM comparison run
  `1edc1c0b-51b8-458d-8826-6da5a1779a7f` matched all 16 compared pages
  (two databases, four capabilities, and two pages per capability), including
  items, pagination state, user, company, model, and record IDs. Its stdout
  SHA-256 is
  `91e0cfcdab32d78ad0deba8a5ec90dc8904cb86a6c2bb275c8eda86757887337`;
  stderr was empty. The retained comparison program SHA-256 is
  `a60cb19c8cb3e4b940c79cfb123c425d15653a62afc88e7c12215a3689228f2f`.
- The journal-entry gate run ID is
  `1b50e4d3-a2d5-4a98-b269-bd3831bfe527`. Its final candidate ran 251 unit
  tests plus four live database/company matrix tests: 255 passed in 99.15
  seconds, with zero pytest stderr. Both databases' 11-table fingerprints,
  the 15 implementation files, and the runtime configuration were unchanged.
  The summary and pytest stdout SHA-256 values are
  `b7825eaad5158074bffa8240dd5c5aa66ef4a3aaed3c58efd6b0f55229bfdb1f`
  and `255bc9ede082c949f7d78844cb044596e5de64974a8587d73cb2cbe1173997bf`.
- An independent Odoo-side comparison matched `journal_entry.search` basic,
  combined-filter, and empty-result reads plus `journal_entry.get` details for
  both databases and both companies. It also confirmed that cross-company and
  absent entry IDs produce the same public `record_not_found` result. Its
  stdout and retained comparator SHA-256 values are
  `69ddecec88c676b7f6be572412f08166c6aac1218117eef56bb9e2f1f57b92ab`
  and `30208e985766431a22940163b424657d1cac80702fe8efa34e72efae52dda54a`.
  The comparator stderr contains only Odoo 19 deprecation/log output (3,191
  bytes), not a comparison failure; its SHA-256 is
  `6c661bc464aa17d7f50ca8b51aae5b299c006e3f2562ef394d646498a6cd1a59`.
- The current journal-entry fixture contains only one entry per company, so
  live verification proves the terminal page but does not claim a real
  second-page cursor traversal. Descending keyset behavior is covered by unit
  contracts until the fixture matrix is expanded.
- The configured-company-context gate run ID is
  `3df1b570-8ec8-4fd5-9894-42e4f752d4a8`. It recorded 272 passing unit tests
  and four passing live tests with zero skips. The live matrix covered both
  database aliases, both selected companies, and two one-record pages per
  request; it verified the configured allowlist intersection, actual Odoo user
  company membership, current-company markers, exact company/currency/country
  values, and non-overlapping ID keyset traversal.
- That gate kept the relevant company, currency, country, user, membership,
  and module fingerprints unchanged in both databases; the runtime config,
  155/153 database count, four service states, Git candidate paths, and
  candidate manifest were also unchanged. Public scanning returned zero
  findings. The gate summary SHA-256 is
  `c424a0d414d8ad0b60aafe352693c44cb8ad9021d678d9393b0428ab6c38a2ef`.
- The same gate built a 43-entry wheel, installed it into a fresh environment,
  and executed the installed `company.accounting_context.list` command against
  `v4-dev`. The wheel SHA-256 is
  `64797392bdf833d08105fe0b44e856c9052004e26366a16c38e17cc81247cd4c`.
  An earlier private gate attempt stopped only because its wheel-entry assertion
  used the wrong installation prefix; the wheel already contained all three
  required resources. The corrected private checker then completed the final
  gate above without changing product files.
- The fixed `report.trial_balance` vertical slice uses
  `account_reports.trial_balance_report`, re-browses the effective
  `account.report`, and calls `get_report_information_readonly` inside the
  bridge's forced read-only transaction. Its gate run ID is
  `81740f4a-0402-41ab-a6a5-c9aba284a2a2`; it covers 296 unit tests and a live
  matrix of both synthetic databases and both configured companies. The
  current fixture proves four schema-valid monetary columns, two account
  lines, and a balanced total of debit `123.45`, credit `123.45`, and balance
  `0`; it does not yet prove high-volume report pagination or cross-period and
  foreign-currency golden cases.
- The fixed `report.balance_sheet` vertical slice uses
  `account_reports.balance_sheet` in Odoo's single-date mode. Both synthetic
  databases and both configured companies returned 21 schema-valid hierarchy
  lines as of 2025-01-31; the current fixture proves Assets equal Liabilities
  plus Equity at `-123.45`. It does not yet prove a multi-period comparison,
  foreign-currency valuation, or a high-volume pagination golden case.
- The fixed `report.profit_and_loss` vertical slice uses
  `account_reports.profit_and_loss` in Odoo's range mode. Both synthetic
  databases and both configured companies returned eight schema-valid lines
  for January 2025; the current fixture proves operating expenses of `123.45`
  and a net loss of `-123.45`. It does not yet prove revenue, cost-of-revenue,
  multi-period comparison, or high-volume pagination golden cases.
- The fixed `report.cash_flow` vertical slice uses
  `account_reports.cash_flow_report` in Odoo's range mode. Both synthetic
  databases and both configured companies returned 19 schema-valid lines for
  January 2025, including a net cash decrease and closing cash balance of
  `-123.45`. Odoo account detail lines that omit `unfoldable` are normalized to
  `false`. The current fixture does not yet prove classified operating,
  investing, and financing cash-flow golden cases or high-volume pagination.
- The fixed `report.tax` vertical slice starts from `account.generic_tax_report`
  and lets Odoo select the company-specific effective report. The CN company
  currently returns the empty two-column generic tax layout, while the SG
  company returns its 28-line GST report; both outcomes were verified in both
  synthetic databases. No tax transactions exist yet, so non-zero tax totals
  and tax-report pagination remain pending fixture coverage.
- The fixed `user.accounting_access.inspect` vertical slice reports the
  configured user's membership in five allowlisted accounting groups and the
  model-level read/create/write/unlink ACL results for six accounting models.
  It is evaluated in the same read-only transaction for both databases and
  both configured companies. These booleans describe model ACLs only; record
  rules and business workflow constraints still apply to individual records.
- The fixed `company.accounting_configuration.inspect` vertical slice reports
  each selected company's currency, country and fiscal country, chart template,
  rounding and fiscal-year settings, account-code prefixes, configured suspense
  and POS receivable accounts, and opening state. Both databases matched the
  expected CN and SG synthetic-company values.
- The fixed `diagnostic.accounting_environment.inspect` vertical slice reports
  the selected company and business user, the installed state of `base`,
  `account`, and `account_reports`, and read access for nine fixed accounting
  runtime models. All four dual-database/company diagnostic calls were
  schema-valid and reported a read-only transaction; it does not expose
  arbitrary model or ACL queries.
- The fixed `partner.accounting.search` vertical slice searches only shared or
  selected-company accounting partners through `res.partner`, with explicit
  customer/vendor roles, name/reference filters, and a database/company/user-
  bound `(complete_name,id)` cursor. Fixture v1 supplies one customer and one
  vendor per company, so both database aliases and both companies exercise one
  real two-page transition; larger duplicate-name and high-volume pages remain
  future fixture coverage.
- The invoice gate run ID is
  `a4fe64be-058e-4eb3-af3f-3161abc44543`. It recorded 547 passing unit tests and
  four passing live tests across both database aliases and both companies, with
  zero skips and empty unit, live, and direct-comparison stderr. All 21 selected
  business-table fingerprints, the 18 implementation files, and the runtime
  configuration were unchanged. The summary SHA-256 and the SHA-256 of the
  retained `SHA256SUMS` manifest are
  `1864b938aa2bd21dcbac60826d2c0dc43529e8f6b94db438e4bf6576a21b0090`
  and `0e36c3dc091be544d316e8e7146452a88e445f0da561c0a0cf154054112cf04f`.
- Its independent Odoo-side comparison matched `invoice.search`, `invoice.get`,
  and `invoice.payment_status.inspect` for both physical databases and both
  companies. It checked invoice and bill pagination, details, partial versus
  unpaid status, explicit partial reconciliations, and payment records. The
  comparison-output and retained comparator SHA-256 values are
  `14604abb728d62f2a50348fa23f3bf52825f03c8df882ad6738b3a5e313be61b`
  and `ba4e27e521ef1dc90c5abd28c3b54c84d66df60f2e80adf0c4f1ef11890f03b7`.
  The current fixture does not claim live coverage for refunds, draft/cancelled
  documents, every payment state, multi-currency documents, or payment-provider
  records without journal entries; those branches remain unit-tested or pending
  richer fixtures.
- Earlier gate run `c16d4163-9979-453a-91e6-a12c3c65d7c1` correctly failed when
  cross-company/not-found errors omitted already verified Odoo context. The
  error contract was fixed and the final run above supersedes that retained
  diagnostic attempt.
- The open-items gate run ID is
  `b7138cf2-1d84-4014-bd41-351c38fe0ac9`. It ran 688 unit tests and eight
  live database/company/capability cases with zero skips and empty test
  stderr. Its independent comparator made 88 formal CLI calls and matched
  every public field, pagination result, and all nine filters against separate
  business-user ORM reads in both physical databases and both companies.
  Database-specific record IDs proved that the two aliases did not collapse
  onto one physical database. All 24 selected business and access-control
  table fingerprints, PostgreSQL endpoint identities, the 15 implementation
  files, runtime configuration, and root-owned read-only execution snapshot
  were unchanged. The summary, direct-comparison output, and retained
  `SHA256SUMS` SHA-256 values are
  `96cc7798fed951121d358961f390d7763efc6b69aa749652905af5a01f7d8ef6`,
  `4869e215eb8cfc301f1cec7f72c73a932a4656d7398efbc5079b6560252886de`,
  and `8a2dc2d3efdbfa191157b646fb9045116997380a21d40518a74ccc7a2816ec73`.
  Fixture v1 has one open item per side and company, so this gate proves
  terminal pages and filter isolation but does not claim a real second-page
  cursor traversal; that branch remains contract- and unit-tested.
- The retained earlier attempt `0ee022d0-ee59-4f95-aeb0-3538aa542a95`
  correctly failed after its unit and live tests passed because its evidence
  comparator ran under the Odoo virtual environment without the control-side
  JSON Schema dependency. The dependency boundary was fixed without changing
  product code, independently reviewed, and the successful run above used a
  new UUID and a newly archived comparator.

## Remaining-read batch

- `report.deferred_expense` and `report.deferred_revenue` use
  `account_reports.deferred_expense_report` and
  `account_reports.deferred_revenue_report` in range mode.
  `report.multicurrency_revaluation` uses
  `account_reports.multicurrency_revaluation_report` in single-date mode and
  retains the real multi-currency group/configuration prerequisite.
- `report.china.balance_sheet`, `report.china.profit_and_loss`, and
  `report.china.cash_flow` use the three `l10n_cn_reports` definitions verified
  on the server. They fail closed unless the selected company has fiscal
  country `CN` and chart `cn_oscg`. `report.singapore.gst` resolves
  `l10n_sg.tax_report` and requires fiscal country `SG` and chart `sg`.
- `fiscal_position.resolve` calls Odoo's native resolver and optional native
  account/tax mappings. `diagnostic.journal_integrity.inspect` returns the
  native company hash-integrity fields; V4 does not expose a generic private
  method call or attempt to reconstruct the hash algorithm.
- Focused tests recorded `169 passed` locally and `169 passed in 154.64s` on
  the server. The complete local suite recorded `1747 passed, 124 skipped in
  512.79s`. The shared read-only server smoke recorded `18 passed in 135.97s`
  for nine commands across `v4-dev` and `v4-e2e`.
- The retained smoke log path is
  `/opt/odoo-accounting-cli-v4/.tooling/remaining-read-batch-live-e8026729-6c8d-401d-8b41-8a6dcfb919f8.log`;
  its SHA-256 is
  `e342e93c397d12549b432f08e4720661d4ac9e7d360d679c0977fc6a0be4bf81`.
- The two deferred reports and multicurrency revaluation currently return
  valid empty results; fiscal-position resolution returns no match for the
  shared partner fixture, and journal integrity returns no rows because the
  fixtures contain no hashed moves. Positive fixtures remain necessary before
  claiming non-empty golden coverage. The four localized reports returned
  their real non-empty layouts in both databases.
- At that checkpoint, bank reconciliation was intentionally deferred until its
  request required a company-scoped bank `journal_id`; otherwise Odoo's handler
  selected a first-journal fallback intended for tests. The later core-object
  read batch below satisfies that condition and supersedes only this exclusion.
  Period-close readiness remains deferred because Odoo 19 has no native
  composite API matching the current descriptor.

## Extended-write batch

- The batch implements `asset.cancel`, `asset.dispose`, `asset.pause`,
  `deferred_expense.generate_entries`, `deferred_revenue.generate_entries`,
  `multicurrency.revaluation.generate_entries`,
  `reconciliation.automatic.run`, `period.transfer.run`, and
  `localization.china.period_transfer.run` through fixed request/response
  schemas and the existing public `write run` contract.
- Asset commands delegate to native lifecycle actions. Deferred and
  revaluation commands use official Odoo handler/wizard paths and return their
  generated move pair. Reconciliation operates on an exact company-scoped
  line set. Transfer commands use native transfer models; the localized
  command additionally verifies the China fiscal country and chart.
- The complete local suite recorded `1854 passed, 125 skipped in 434.28s`.
  The synchronized server-focused suite recorded `301 passed in 149.06s`.
- One guarded shared smoke ran every command and its immediate replay through
  the public contract in both `v4-dev` and `v4-e2e`: `1 passed in 10.46s`.
  The one pytest case contains the complete two-alias matrix.
- The smoke used one shared transaction, rolled it back, then opened a fresh
  read-only cursor to prove the tracked assets, moves, and move lines absent
  and the temporary deferred-company settings restored. No service, existing
  business database, V2/V3 installation, or Odoo source/add-on was modified.
- Evidence remains deliberately narrow: cancel/dispose used zero-value assets;
  Odoo exposes no reliable persisted pause-date field; and visible replay
  markers for the deferred/revaluation move pairs and both transfers are not
  database-unique under concurrency. The affected capabilities are marked
  `degraded` instead of claiming concurrent exactly-once execution.

## Historical core-object read batch

- Eleven new fixed capabilities are handler-backed:
  `account.account.get`, `journal.get`, `tax.get`, `payment_term.get`,
  `currency.get`, `partner.accounting.get`, `bank.transaction.get`,
  `journal_item.search`, `journal_item.get`, `payment.method.list`, and
  `reconciliation.model.list`. The previously disabled
  `report.bank_reconciliation` is also enabled, for twelve commands in this
  batch and 66 read handlers at the historical 113-ID checkpoint.
- The commands expose only fixed contracts and ORM models under the configured
  business user and selected company. Bank reconciliation requires an explicit
  company-scoped bank `journal_id`, so Odoo's arbitrary first-journal fallback
  is not reachable through the public request.
- Focused contract, schema, bridge, runtime, CLI, and registry tests pass. The
  clean full local regression passed 2052 tests with 127 opt-in live tests
  skipped in 1086.74 seconds; the synchronized server-focused selection passed
  258 tests in 110.05 seconds.
- The guarded read-only `v4-dev` run recorded `1 passed in 154.45s`; the
  guarded read-only `v4-e2e` run recorded `1 passed in 152.27s`. Each pytest
  case exercised all twelve commands in its named isolation database. Together
  they raised retained live success evidence at that checkpoint to 65 reads
  and 25 writes.
- Odoo 19 returned `account.payment.term.line.days_next_month` as a numeric
  string during the first live run. The response validator and schema now
  accept that validated representation, and the repeated live runs pass.
- No command in this batch performed a business write. No Odoo, Nginx,
  PostgreSQL, Pi, V2, or V3 service was restarted, and no existing business
  database, Odoo source/add-on, or legacy installation was modified.

## Reference-object read batch

- Twelve new fixed capabilities are handler-backed: `product.search`,
  `product.get`, `analytic.plan.list`, `analytic.plan.get`,
  `analytic.account.search`, `analytic.account.get`, `fiscal_position.search`,
  `fiscal_position.get`, `account.tag.list`, `account.tag.get`,
  `tax.group.list`, and `tax.group.get`. They brought the registry at that
  checkpoint to 125 IDs and 104 handlers, comprising 78 reads and 26 writes; 21
  descriptors remained disabled.
- The six collection/search and exact-ID pairs reuse the closed core-object
  contract, fixed schemas and ORM models, company/configured-user scope,
  ordinary-accountant ACL gates, and keyset cursor behavior. No arbitrary model
  or method dispatcher is exposed.
- The focused local selection passed 382 tests in 78.94 seconds. The final
  complete local regression passed 2208 tests with 129 opt-in live tests
  skipped in 827.35 seconds. The synchronized server-focused selection passed
  382 tests in 118.32 seconds.
- The guarded transactional live smoke covered all twelve commands in both
  `v4-dev` and `v4-e2e` and recorded `2 passed in 6.71s`, taking retained live
  success evidence to 77 reads and 25 writes. Temporary product,
  analytic-account, and fiscal-position fixtures were created only inside the
  transaction, the whole transaction was rolled back, and a fresh cursor proved
  zero residue.
- The first live run exposed the legitimate Odoo value `categ_id=False`.
  Product `category` normalization and schema now accept a named reference or
  `null`, and the repeated live run passed.
- The registry capability-ID SHA-256 is
  `cd8d1f5672ec9d779875389c00ec3276a890927cb5b5543a67ed840cf714736d`;
  the verified wheel SHA-256 is
  `a7f5e50cde6b10bd75c515463db3bc57387c845bc3afa3941cd52f625a123f96`.
  No persistent business write occurred, and no Odoo, Nginx, PostgreSQL, Pi,
  V2, or V3 service was restarted or modified.

## Accounting-configuration read batch

- Eight new fixed capabilities are handler-backed: `payment.method.get`,
  `reconciliation.model.get`, `cash_rounding.list`, `cash_rounding.get`,
  `journal.group.list`, `journal.group.get`, `incoterm.list`, and
  `incoterm.get`. They bring the registry to 133 IDs and 112 handlers,
  comprising 86 reads and 26 writes; 21 descriptors remain disabled.
- These commands reuse the closed core-object contract, fixed schemas and ORM
  models, configured-user/company scope, ordinary-accountant ACL gates, and
  keyset cursor behavior. No arbitrary model or method dispatcher is exposed.
- The final focused local selection passed 484 tests in 86.71 seconds. The
  complete local regression passed 2310 tests with 131 opt-in live tests
  skipped in 526.90 seconds. The synchronized server-focused selection passed
  484 tests in 124.70 seconds.
- The guarded transactional live smoke covered all eight commands in both
  `v4-dev` and `v4-e2e` and recorded `2 passed in 6.41s`, taking retained live
  success evidence to 85 reads and 25 writes. Temporary cash-rounding and
  journal-group fixtures were rolled back with the whole transaction, and a
  fresh cursor proved zero residue.
- The registry capability-ID SHA-256 is
  `91dc2d269fd70d4b0badfae1e0a255750a9201ffb6c1194f013672503256fb64`;
  the verified wheel SHA-256 is
  `bf280a27dac85f681f2fd9767f771d68f08d7f3507c8b71cabafa8efc3206476`.
  No persistent business write occurred, and no Odoo, Nginx, PostgreSQL, Pi,
  V2, or V3 service was restarted or modified.

## Historical payment/reconciliation object read batch

- Eight new fixed capabilities are handler-backed:
  `partner.bank_account.search`, `partner.bank_account.get`,
  `bank.statement.search`, `bank.statement.get`, `reconciliation.partial.list`,
  `reconciliation.partial.get`, `reconciliation.full.list`, and
  `reconciliation.full.get`. They bring the registry to 141 IDs and 120
  handlers, comprising 94 reads and 26 writes; 21 descriptors remain disabled.
- These commands reuse the closed core-object contract, fixed schemas and ORM
  models, configured uid 5, ordinary-accountant ACL gates, company scope, and
  ID-keyset cursor behavior. Partial and full reconciliation relations are
  normalized and cross-checked without exposing arbitrary model/method access.
- The final focused local selection passed 618 tests in 77.48 seconds. The
  complete local regression passed 2444 tests with 133 opt-in live tests
  skipped in 492.22 seconds. The synchronized server-focused selection passed
  618 tests in 134.89 seconds.
- The guarded transactional live smoke covered all eight commands in both
  `v4-dev` and `v4-e2e` and recorded `2 passed in 6.80s`, taking retained live
  evidence to 93 reads and 25 writes. Temporary partner, bank-account, and
  bank-statement fixtures were rolled back, a fresh cursor proved zero marker
  residue, and the existing bank line was restored to its original unbound
  state. Existing partial and full reconciliations supplied positive rows.
- The registry capability-ID SHA-256 is
  `9d0dd71415220c7a05e6a1e600f56d811459c978d840aee91d14cb94b6af66c6`;
  the verified wheel SHA-256 is
  `20c663f1637c35c0433d08c364b920c784296f3854e21cddc0cc96217c2c7c85`.
  No persistent business write occurred, and no Odoo, Nginx, PostgreSQL, Pi,
  V2, or V3 service was restarted or modified.

## Previous analytic/budget read batch

- Eleven new fixed capabilities are handler-backed: `analytic.line.search`,
  `analytic.line.get`, `analytic.distribution_model.list`,
  `analytic.distribution_model.get`, `analytic.applicability.list`,
  `analytic.applicability.get`, `budget.search`, `budget.get`,
  `budget.line.list`, `budget.line.get`, and `report.budget`. They bring the
  registry to 152 IDs and 131 handlers, comprising 105 reads and 26 writes; 21
  descriptors remain disabled.
- The first ten commands reuse fixed schemas and the closed core-object ORM
  path. `report.budget` uses a dedicated fixed action over the official
  SQL-backed `budget.report` model. Odoo record rules still define the base
  query; parameterized SQL preserves duplicate text row keys, and the public
  composite cursor prevents overlapping budget lines from being merged.
- An independent read-only audit found and prompted one correction before
  server promotion: analytic account membership now follows Odoo 19
  `plan_id child_of` semantics. Root-to-child membership succeeds and sibling
  membership fails in regression coverage. The audit found no other blocker.
- Targeted lint and format checks passed. All 267 JSON Schema documents passed
  Draft 2020-12 validation. The final focused local selection passed 825 tests
  with 2 guarded live cases skipped in 96.40 seconds; the final complete local
  regression passed 2651 tests with 135 opt-in live tests skipped in 553.11
  seconds; the synchronized server-focused selection passed 825 tests in
  164.16 seconds.
- The guarded transactional live smoke covered all eleven commands in both
  `v4-dev` and `v4-e2e` and recorded `2 passed in 7.54s`, taking retained live
  evidence to 104 reads and 25 writes. It verified two rows for one analytic
  line under overlapping budget lines, one-item report pagination, configured
  uid 5 execution, whole-transaction rollback, and fresh-cursor zero residue.
- The registry capability-ID SHA-256 is
  `f06fde8711e20a7e4a2853301707e946bb49fe38aec65dcb6c836e14d96de46b`;
  the verified wheel SHA-256 is
  `0f2f8c3ae75c0a0c8c07e88322bdab0e7a8381df0f2db11e8c4f34d153de729f`.
  No persistent business write occurred, and no Odoo, Nginx, PostgreSQL, Pi,
  V2, or V3 service was restarted or modified.

## Previous document-lifecycle write batch

- Eight fixed capabilities are handler-backed: `invoice.update`,
  `invoice.lines.replace`, `invoice.cancel`, `invoice.reset_to_draft`,
  `journal_entry.update`, `journal_entry.lines.replace`,
  `journal_entry.cancel`, and `journal_entry.reset_to_draft`. They bring the
  registry to 160 IDs and 139 handlers, comprising 105 reads and 34 writes; 21
  descriptors remain disabled.
- The update and full-line-replacement commands accept closed field contracts.
  They preserve Odoo company, move-type, ACL, group, and draft-state rules.
  Cancel and reset use Odoo's native `button_cancel` and `button_draft`
  methods; no arbitrary model, field, or method dispatcher is exposed.
- New invoice, bill, and journal-entry create markers no longer occupy the
  business `ref` field. The compatibility marker is stored in
  `invoice_origin`, while replay of legacy `ref` markers remains supported.
  This is a pragmatic compatibility mechanism, not a proof of concurrent
  exactly-once execution or a permanent operation store.
- The focused local selection passed 326 tests. The complete local regression
  passed 2732 tests with 136 opt-in live tests skipped in 598.63 seconds. The
  synchronized server-focused selection passed 326 tests in 302.59 seconds.
  All 283 canonical schemas passed Draft 2020-12 validation.
- One shared guarded transactional smoke exercised all eight commands, their
  create/post prerequisites, replay after assigning a business reference, and
  rollback isolation in both `v4-dev` and `v4-e2e`; it recorded
  `1 passed in 10.10s`. A fresh superuser cursor proved that all created IDs
  were absent after rollback. Retained live evidence is now 104 reads and 33
  writes.
- The capability-ID SHA-256 is
  `a9ef0676628249f342fbdf580f85fdca85d4bb1e68e8f48fd026b2627fb43b47`;
  the registry-file SHA-256 is
  `71398c69324d16a26a3b5efa79577197a3db51c495f3d3726a0ecde9e20e2ee9`;
  the verified wheel SHA-256 is
  `b3e6be281ef21dad90bc96994e09d9acf9360648a4a1dffddeca268d63ea3ba5`.
  No persistent business write occurred, and no Odoo, Nginx, PostgreSQL, Pi,
  V2, or V3 service was restarted or modified.

## Previous payment/bank reconciliation batch

- Ten fixed capabilities are handler-backed: `bank.transaction.search`,
  `bank.transaction.reconciliation.get`,
  `bank.transaction.match_candidates.list`, `payment.create`,
  `payment.update_draft`, `payment.reset_to_draft`, `bank.transaction.update`,
  `bank.transaction.match`, `bank.transaction.unmatch`, and
  `reconciliation.write_off`. They bring the registry to 170 IDs and 149
  handlers, comprising 108 reads and 41 writes; 21 descriptors remain disabled.
- The three reads and seven writes use closed schemas and allowlisted ORM
  actions. Ordinary uid/company scope, Odoo ACLs, exact write confirmation,
  idempotency, and isolation-database restrictions remain intact; no arbitrary
  model, field, or method dispatcher is exposed.
- The focused local selection passed 72 tests, and the complete local regression
  passed 2802 tests with 137 opt-in live tests skipped. The synchronized
  server-focused selection also passed 72 tests. All 303 canonical schemas are
  present in the clean wheel.
- One shared guarded transactional smoke exercised all ten commands in both
  `odoo_cli_v4_dev` and `odoo_cli_v4_e2e` as configured uid 5/company 1 and
  recorded `1 passed in 10.78s`. Whole-transaction rollback and fresh-cursor
  checks proved that the temporary payment, invoice, and bank-transaction
  fixtures left no residue. Retained live evidence is now 107 reads and 40
  writes.
- The capability-ID SHA-256 is
  `d7685dbfd2461eb436593566ece52efdbf0120fbea00aeeceafed864e54c120e`;
  the registry-file SHA-256 is
  `95a41290f9feb72e3b786247834a131635ca003144b4bdd0c766f5d69cfef618`.
- The synchronized source archive is 574618 bytes with SHA-256
  `cae7e6fe2aff2f27751282803d1cf2daeb80f149388450186f0d13136a5fe994`.
  The clean wheel is 574779 bytes with SHA-256
  `e5c185984a198fda856b1caa921373e91e4a3c2d4ee8c3c0dfd28fb37315fc09`;
  it contains 303 schemas and all 170 capabilities, with no backslash-named
  entries.
- No persistent business write occurred, and no business database, Odoo source,
  V2/V3 chain, or service was modified or restarted.

## Previous analytic/budget write batch

- Nine fixed writes are handler-backed: `analytic.account.create`,
  `analytic.account.update`, `budget.create`, `budget.update_draft`,
  `budget.lines.replace`, `budget.confirm`, `budget.reset_to_draft`,
  `budget.cancel`, and `budget.mark_done`. They bring the registry to 179 IDs
  and 158 handlers, comprising 108 reads and 50 writes; 21 descriptors remain
  disabled.
- The commands use closed schemas and allowlisted ORM actions over
  `account.analytic.account`, `budget.analytic`, and `budget.line`. Native Odoo
  budget states and transitions remain authoritative. Ordinary uid/company
  scope, Odoo ACLs, exact write confirmation, deterministic replay checks, and
  isolation-database restrictions remain intact; no arbitrary dispatcher is
  exposed.
- The focused local selection passed 386 tests, the synchronized server-focused
  selection passed 386 tests, and the final server runtime selection passed 79
  tests. The complete local regression passed 2,926 tests with 138 explicitly
  gated live tests skipped and zero failures in 1,927.74 seconds.
- One shared guarded transactional smoke exercised all nine commands in both
  `odoo_cli_v4_dev` and `odoo_cli_v4_e2e` as configured uid 5/company 1. Every
  command completed a first execution and an immediate deterministic replay;
  pytest recorded `1 passed in 6.09s`. Whole-transaction rollback and a fresh
  cursor proved zero residual analytic accounts, budgets, and budget lines.
  Retained live evidence is now 107 reads and 49 writes.
- The capability-ID SHA-256 is
  `41d06b41aa49596ad0f67d9c343761af0918c68c902c1046bcf0d1684e2ddcda`;
  the registry-file SHA-256 is
  `72b03d16135e337c2407b05aa77df265c143c8f6f95e3a6a75ece2fffbc225dd`.
- The synchronized source archive is 132872 bytes with SHA-256
  `18e0c881861e770754ccca8e055f804e0cd0d464d0f6d9678c4f72ad6db24ea1`.
  The clean wheel is 595273 bytes with SHA-256
  `b93dd572f61d07953645b300c67a846b565af1514d90dab8ad8a3970b15e341e`;
  it contains 392 entries, 321 schemas, all 179 capabilities, and all 158
  handlers, with no backslash-named entries.
- Thirty-six malformed schema-name duplicates and 28 malformed project-name
  duplicates were moved recoverably to
  `/tmp/odacv4-malformed-schema-names-20260826` and
  `/tmp/odacv4-malformed-project-names-20260826`; the active project tree now
  contains zero backslash-named duplicates.
- `analytic.account.create` and `budget.create` are `degraded`: their visible
  markers lack a database uniqueness constraint, so concurrent exactly-once
  creation is not proven. The other seven writes use deterministic keys plus
  current/target-state rechecks but no operation store; an old request may
  execute again after an intervening state change.
- No persistent business write occurred, and no business database, Odoo source,
  V2/V3 chain, or service was modified or restarted.

## Preceding partner master-data batch

- Two fixed reads and nine fixed writes are handler-backed: `partner.search`,
  `partner.get`, `partner.create`, `partner.update`, `partner.archive`,
  `partner.restore`, `partner.accounting.update`,
  `partner.bank_account.create`, `partner.bank_account.update`,
  `partner.bank_account.archive`, and `partner.bank_account.restore`. They bring
  the registry to 190 IDs and 169 handlers, comprising 110 reads and 59 writes;
  21 descriptors remain disabled.
- The commands use closed schemas and fixed ORM actions over `res.partner` and
  `res.partner.bank`. Configured uid/company scope, Odoo ACLs, exact write
  confirmation, deterministic replay checks, and isolation-database
  restrictions remain intact; no arbitrary dispatcher is exposed.
- The final post-fix partner selection passed 98 tests locally; the guarded live
  case was skipped locally because it was not authorized. The synchronized
  server-focused selection passed 428 tests in 84.24 seconds; the final
  post-sync registry evidence selection passed 24 tests in 81.97 seconds.
- The complete local regression passed 3,060 tests with 139 explicitly gated
  live tests skipped and zero failures in 1,687.53 seconds (28:07).
- One shared guarded transactional smoke exercised all eleven commands in both
  `odoo_cli_v4_dev` and `odoo_cli_v4_e2e` as configured uid 5/company 1. Every
  command completed a first execution and an immediate replay; pytest recorded
  `1 passed in 7.03s`. The fixture temporarily linked
  `base.group_partner_manager` inside the rollback-only transaction; this does
  not claim that the normal configured user has that group.
- Whole-transaction rollback and fresh-cursor SQL checks proved zero residual
  partner, bank-account, or temporary user-group rows. Retained live evidence
  is now 109 reads and 58 writes.
- The capability-ID SHA-256 is
  `c42bdb67c1a540293d91ef5e84d1a4e0291a36a2b5eb8e7261660cf73dc7238a`;
  the registry-file SHA-256 is
  `5583f4b43a7532774b3e8c3365205321b6ed38be8fc9aaaafc4868d1a3fae181`.
- The final 42-entry source archive is 199107 bytes with SHA-256
  `f6364dadeb9fe53a91cc52685b52218e4f546da8863701a972a60ffcc9203c8c`;
  every archived file is byte-identical to the final active-tree file, with no
  duplicate or backslash-named entries. The clean wheel is 622561 bytes with
  SHA-256
  `dce638bc8e9b15a60e39f6045868ff277be4545ef52697bf283da065fac38388`;
  it contains 414 entries, 343 schemas, all 190 capabilities, and all 169
  handlers, with no duplicate or backslash-named entries.
- `partner.create` is `degraded`: its visible `ref` marker lacks a database
  uniqueness constraint, so concurrent exactly-once creation is not proven.
  The other eight writes use deterministic target-state rechecks but no
  operation store; an old request may execute again after an intervening state
  change.
- Odoo without the optional `phone_validation` module may not expose the
  `res.partner.mobile` field. Reads keep the stable `mobile: null` output;
  writes accept null/omission and fail closed for a non-null value.
- No persistent business write occurred. This batch issued no service restart
  and modified no business database, Odoo source, or V2/V3 chain. A later
  2026-08-27 06:23 Odoo restart was traced to
  `apt-daily-upgrade`/`needrestart` after an OpenSSL upgrade; the service stopped
  cleanly, and the audit found no V4 deployment relation.

## Previous accounting-configuration write batch

- Twelve fixed writes are handler-backed: `account.account.create`,
  `account.account.update`, `account.account.archive`,
  `account.account.restore`, `journal.create`, `journal.update`,
  `journal.archive`, `journal.restore`, `tax.create`, `tax.update`,
  `tax.archive`, and `tax.restore`. They bring the registry to 202 IDs and 181
  executable handlers, comprising 110 reads and 71 writes; 21 descriptors
  remain disabled. Retained live success evidence is now 109 reads and 70
  writes.
- The commands use closed schemas and fixed ORM routes over `account.account`,
  `account.journal`, and `account.tax`, with declared `res.company`,
  `res.currency`, `account.tax.group`, and account-reference dependencies. All
  twelve require `account.group_account_manager`, configured
  database/company/user scope, exact confirmation, native Odoo ACLs, and
  deterministic replay checks. No arbitrary model, field, or method dispatcher
  is exposed.
- Account creation fixes `company_ids` to the requested company. Account
  update, archive, and restore fail closed when `company_ids` contains any
  other company, so a single-company request cannot mutate a shared chart
  record. `journal.create` also preflights `account.account:create`, because
  Odoo can create a default liquidity or credit-card account when a bank, cash,
  or credit journal omits `default_account_id`; native automatic assignment is
  accepted by create replay. Tax amounts are limited to at most four decimal
  places, matching Odoo 19's `digits=(16, 4)` storage instead of accepting a
  value that Odoo would round after validation.
- `account.account.create` and `tax.create` are honestly `degraded` with
  `concurrent_idempotency_limit`: company-scoped natural keys and deterministic
  request keys support ordinary first execution and immediate replay, but no
  database-unique request marker proves concurrent exactly-once creation. The
  remaining ten writes use fixed natural-key or current-target-state rechecks
  without an operation store.
- The earlier broad server selection is retained only as a pre-fix baseline:
  `423 passed in 767.33s`. After the final company-scope, automatic-journal-
  account, ACL, and tax-contract corrections, the final focused server
  selection passed `103 tests in 61.13 seconds`. The complete local regression
  recorded `3186 passed, 140 skipped, 1 failed in 3314.58s`; its only failure
  was a stale CLI-contract assertion for the preceding 190-ID count. After that
  test-only expectation was corrected to 202, the full CLI-contract file passed
  `14 tests in 167.25s`. No second 55-minute full sweep was run after changing
  only that assertion, and no inferred 3187-pass result is claimed.
- One guarded transactional smoke exercised all twelve commands in both
  `odoo_cli_v4_dev` and `odoo_cli_v4_e2e` as configured uid 5/company 1 and
  recorded `1 passed in 9.55s`. Every command completed a first execution and
  an immediate replay. The fixture temporarily linked
  `account.group_account_manager` only inside the rollback transaction. A fresh
  cursor proved that the created account, bank journal, tax, and journal-
  generated liquidity-account IDs were all absent and that the temporary user-
  group relation was rolled back.
- The capability-ID-list SHA-256 is
  `6ecb58789446447a2d3e4d89957eb6cd87147b0346fd68a6fa3cef23b5dc08f3`;
  the registry-file SHA-256 is
  `5f224b7661ad07844b2cebb694fb5e864eb78dbcbefc2b9e1b91c28c0e97e81a`;
  and the canonical registry digest is
  `91d087511423deb5c7aede88ec28452468ec570b44b8a8d79649e7cdbf8563d0`.
  The clean 438-entry wheel is 649140 bytes with SHA-256
  `37646e68151cbbfcefe70f5e3e74df0919b1dd6a3e18c50876a5a95f7b1dbf55`;
  it contains all 367 schemas, 202 capabilities, and 181 handlers. The
  synchronized 58-file source archive is 158280 bytes with SHA-256
  `f51cdcf1da0ff7d85f811cff08cb27866c00871549c55972546f8b2adec453ee`.
- No persistent business write occurred. This batch modified no business
  database, Odoo source tree, V2/V3 installation, or service and issued no
  service-restart command. At 2026-08-27 10:25:26 the existing Odoo main process
  crossed its configured 2 GiB VMS soft limit and initiated a phoenix reload.
  `_reexec()` resolved basename `python` outside the virtual environment, failed
  to import the venv-only `passlib`, and exited; systemd's existing
  `Restart=always` policy recovered the service at 10:25:38. The same failure
  pattern is present on prior dates. Repository and artifact audits found no
  service-control, signal, package-install, or persistent `PYTHONPATH` path in
  this batch; the operational defect was documented but not changed.

## Previous accounting-depth batch

- This checkpoint deepens 12 existing interfaces (11 writes and one read) and
  adds no capability ID, handler, or schema file. The registry remains at 202
  IDs and 181 handlers (110 reads and 71 writes), with 367 schemas, 21 disabled
  descriptors, and retained live-success counts of 109 reads and 70 writes.
- The deepened interfaces are `customer_invoice.create`,
  `vendor_bill.create`, `invoice.lines.replace`,
  `customer_credit_note.create`, `vendor_refund.create`,
  `journal_entry.create`, `journal_entry.lines.replace`,
  `receivable.payment.register`, `payable.payment.register`,
  `invoice.payment_status.inspect`, `reconciliation.apply`, and
  `reconciliation.undo`.
- Invoice and bill creation now exposes payment terms or due dates and business
  references. Invoice, bill, and refund lines support product, discount, and
  optional analytic distribution. Journal-entry creation/replacement supports
  references, foreign-currency amount pairs, and optional analytic
  distribution. Payment registration accepts a positive partial amount, and
  payment-status inspection returns validated outstanding items.
  Reconciliation apply and invoice-targeted undo use Odoo's native invoice-
  widget operations and preserve unrelated partial reconciliations; multi-term
  graph preservation is covered by contract/runtime tests but not a positive
  multi-term live fixture. The legacy exact two-line apply/undo contract remains
  compatible.
- The final current-tree selection over `test_core_writes.py`,
  `test_core_writes_runtime.py`, `test_invoices.py`, and
  `test_invoice_runtime.py` passes all 496 tests in 826.41 seconds. The current
  registry file independently passes 17 tests in 197.19 seconds. The final
  synchronized invoice/runtime critical selection passed 56 tests in 0.55
  seconds, and the six directly affected public undo tests passed in 4.44
  seconds. The earlier cross-layer selection passed 215 tests in 266.76 seconds
  and the earlier synchronized focused selection passed 200 tests in 93.97
  seconds. These are scoped results; no inferred 496-test server run is claimed.
- One guarded pytest case ran the real Odoo chain in both
  `odoo_cli_v4_dev` and `odoo_cli_v4_e2e` as uid 5/company 1 and recorded
  `1 passed in 16.91s`. All 11 target writes completed first execution and
  immediate replay. Payment status was inspected before assignment, after
  assignment, and after targeted undo. Each alias used one outer transaction;
  rollback plus a fresh read-only cursor proved the recorded move, payment, and
  product-fixture IDs plus markers absent.
- Product data was created only as a superuser-owned smoke fixture inside the
  rollback transaction; every capability handler still ran as uid 5 without
  `sudo` or temporary group elevation. Optional analytic distribution has
  contract/runtime coverage but no positive live analytic fixture because uid
  5 lacks the analytic group. The registry declares only the fixed models and
  ACLs actually touched by each request.
- The capability-ID-list SHA-256 remains
  `6ecb58789446447a2d3e4d89957eb6cd87147b0346fd68a6fa3cef23b5dc08f3`;
  the registry-file SHA-256 is
  `c2f0fe18d1646b0b218fe3860400a26ae997f691291e438eef684acd377db5be`;
  and the canonical registry digest is
  `6eb4404cf3f31d7ce140db41f0346444a6edcd85d7e9720ff9163760623f02b2`.
- The explicit 23-file batch source archive is 201732 bytes with SHA-256
  `de9bf4ccce8f76502d835e001f2880d346dc0b8dcd77264e556c74302d179c36`;
  all entries are unique, safe regular files and byte-identical to the active
  tree. The wheel is 660021 bytes with SHA-256
  `7f8ab0ba3b6e9ac877d74ca9855df1a720248f43631db0d60c7b1071447baa00`.
  Its 438 unique entries contain 367 schemas, all 202 capabilities, and 181
  handlers. A clean install successfully returned 202 capabilities and the
  same canonical registry digest from `capabilities list`.
- The 23 active server files match the local tree byte-for-byte. The source
  archive and wheel are retained under the server project's `.tooling`
  directory with matching hashes. This batch issued no service-control or
  restart command. A post-smoke read-only check found `odoo19`, Nginx, and the
  `postgresql@16-main` cluster active; their start times did not change during
  this batch.

## Historical 236-ID sales/purchase order-document read batch

- Added `sale.order.search`, `sale.order.get`, `sale.order.line.search`,
  `sale.order.analysis.summary`, `purchase.order.search`, `purchase.order.get`,
  `purchase.order.line.search`, and `purchase.order.analysis.summary`.
- The registry has 236 IDs and 215 handlers (144 reads and 71 writes), 435
  schemas, and statuses of 202 `unconfigured`, 13 `degraded`, and 21
  `disabled`. Live evidence covers 143 reads and 70 writes (213 total).
- Local focused tests passed 126 cases; the synchronized server selection
  passed 112. The shared dual-database smoke recorded `2 passed in 7.26s`.
  Independent SQL checks found zero partner, product, sale-order, sale-line,
  purchase-order, and purchase-line residue in both isolated databases.
  Before/after service snapshots were identical: Odoo `NRestarts=2`, Nginx
  `0`, and PostgreSQL `0`.
- Capability-ID-list SHA-256:
  `9032699a10ce3113c27e8ef538d180b331be143edaa365abb79bc0b3702c7232`;
  canonical registry digest:
  `284657bf0e4292039cb85d15767c08ce0bf77c415cfb62addaffa8b123525a2f`;
  registry-file SHA-256:
  `94c9b07f09ea72422c6d30626a330586d3f4bcfa3782ea9a159a9532705c3930`;
  CLI-list-file SHA-256:
  `43aa2ceaefbb34d7b55b4fc79e4a0e5e0b202fb40e21c1222bea7d0d6ad51604`.
- Live JUnit SHA-256:
  `3e6c5463b10057ec287b0a5f3b2cccdce4167797a242bc4971e15389b979dfd1`;
  pre-live archive SHA-256:
  `fe89d7cc0a6351e50e1639ebb0aafa82d63fa6b57962fa5d84cc4ab7272255e9`.

## Historical sales/purchase order-write batch

- Added `sale.order.create`, `sale.order.update_draft`,
  `sale.order.lines.replace`, `sale.order.confirm`, `sale.order.cancel`,
  `sale.order.reset_to_draft`, `purchase.order.create`,
  `purchase.order.update_draft`, `purchase.order.lines.replace`,
  `purchase.order.confirm`, `purchase.order.cancel`, and
  `purchase.order.reset_to_draft` through the shared fixed core-write contract
  and allowlisted company/user-scoped native Odoo runtime.
- The registry has 248 IDs and 227 handlers (144 reads and 83 writes), 459
  schemas, and statuses of 212 `unconfigured`, 15 `degraded`, and 21
  `disabled`. Live evidence covers 143 reads and 82 writes (225 total).
- The focused local selection passed `135 tests in 109.91s`. The repaired
  regression passed `428 tests in 567.86s`; registry/runtime alignment passed
  `44 tests in 4.07s`; and cumulative counts passed `5 tests in 45.46s`. The
  server focused and count selections passed `136 tests in 68.52s` and `5
  tests in 36.47s`. Dual-database evidence recorded `2 passed in 7.74s`, with
  another successful run at `2 passed in 8.29s`.
- Each isolated database retained zero temporary partners, products, sale
  orders, sale lines, purchase orders, purchase lines, and temporary standard-
  group memberships. Odoo 19, Nginx, and PostgreSQL before/after snapshots
  were byte-identical: `NRestarts=2/0/0`.
- Capability-ID-list SHA-256:
  `383bc6b03694c40eeef978244f28f6e28e2440905e98c43e02274726db9f9d25`;
  canonical registry digest:
  `5d10bd54a83a2d375f458fc1c8800ca0691068c054b3f9927dd00a03fba942c3`;
  registry-file SHA-256:
  `6f9caa3efc7c2c6d46d3a1ba6aa801a58e598590a9f77490e6506f036cc82d22`;
  107962-byte CLI-list SHA-256:
  `509015066be34db397e1b0d623da791f435837a98a607adec384ec31ab8a4f07`.
- Live-log SHA-256:
  `8cb502a4b7c5a493a16646f81911231c96931da21d00488037de53b89f1d3a69`;
  JUnit SHA-256:
  `bbacbeaa3b5aaa0ebd2e61e547565ee8eaff40bd6d5cca567b7a3d6383962c18`;
  identical service-snapshot SHA-256:
  `89933ce14d537588ccfa76126b7d1c7b476cbd44a2ff7ae68a1808ffe1b452ef`;
  each residue-file SHA-256:
  `b9e038a67cb826e6fe86c15a414a3d485e2c7b95acce772508f6f66a7a8ef11c`.
- The final synchronized archive contains 49 members, is 205556 bytes, and has
  local/server SHA-256
  `49c0a091fc8a2f10763acd05833bb20f42e3c1df6de0cdcff81c7a5bc3fd96e2`.

## Historical accounting follow-up write batch

- Added or enabled `purchase.order.bill.create`, `purchase_bill.match`,
  `purchase_bill.lines.unmatch`, `payment_term.create`, `payment_term.update`,
  `payment_term.lines.replace`, `payment_term.archive`, `payment_term.restore`,
  and `period.accrual.generate`.
- At that checkpoint the registry had 255 IDs and 236 handlers (144 reads and 92 writes), 477
  schemas, and statuses of 218 `unconfigured`, 18 `degraded`, and 19 `disabled`.
- The shared dual-database smoke passed `2 tests in 8.40s`; each database
  completed nine first executions and nine immediate replays, with transaction
  rollback and temporary-group rollback both true.
- The final focused local selection passed 123 tests; the synchronized server
  selection passed 121. The server public capability list returned all 255 IDs
  and the canonical digest below. Focused tests verify the public CLI/model
  mapping; the rollback-only live smoke verifies the public contract and real
  runtime rather than spawning a second committing CLI process.
- The result contract was corrected for Odoo 19 draft vendor bills, whose
  native `name` is null.
- Capability-ID-list SHA-256:
  `af1c980ecac6e516ed988b152f7a69158ec7523f4e9d7e1cbfba874f5e4c8f0d`;
  canonical registry digest:
  `c8fff1a9975d6adcc23d32b1e5a39fd2b6ae5be6de6a8961d5bb61f0f9565b1f`.
- Odoo PID `3547689`, Nginx master PID `2193677`, PostgreSQL PID `2193725`, and
  Pi bridge PID `3296254` retained their pre-deployment start times; no service
  was restarted.
- This checkpoint does not mark G5 or the overall capability target complete.

## Historical 228-ID operational-inventory read batch

- Added `product.category.list`, `warehouse.list`, `stock.location.list`,
  `stock.operation_type.list`, `stock.route.list`, `stock.transfer.search`,
  `stock.transfer.get`, `stock.move.search`, `inventory.on_hand.summary`, and
  `inventory.availability.inspect` through fixed contracts, CLI dispatch, two
  allowlisted bridge actions, and native Odoo runtime routes.
- The current registry has 228 IDs and 207 handlers (136 reads and 71 writes).
  Live success evidence covers 135 reads and 70 writes. There are 21 disabled
  descriptors, 419 schema documents, and statuses of 194 `unconfigured`, 13
  `degraded`, and 21 `disabled`.
- The synchronized remote selection passed 138 tests. The final shared guarded
  live run executed all ten reads in both `odoo_cli_v4_dev` and
  `odoo_cli_v4_e2e` as uid 5/company 1 and recorded `2 passed in 5.49s`.
  Independent post-rollback residue checks returned `0|0|0|0` for each
  database.
- Two earlier attempts did not form evidence. The root-side attempt stopped at
  peer authentication. The first Odoo-user run failed because the fixture had
  `categ_id=False`; its transaction rolled back and committed nothing. The
  corrected fixture then produced the passing dual-database run.
- The CLI returned 228 unique, sorted capability IDs. Capability-ID-list
  SHA-256:
  `709e4ce12c7d8cf5dcfebb9dbf45a6082aff1ebca380f73aec823d70a1d3088f`.
  Registry-file SHA-256:
  `3e357d2cf4ba748ff4175603dd4c8daf1b6b635efb948291da63b06b7e965391`.
  Canonical registry digest:
  `bc644e686f863ae6cca947c4c040bfecce7395d1c84e9dd8a43b9c83984d7b94`.
- The retained live log is
  `/opt/odoo-accounting-cli-v4/.tooling/inventory-read-live-run-1787885244043.log`
  with SHA-256
  `b8b6afa9754d7f32236ba2fa43fdd01f66b9523f2fdeccc0601ee542d969bc6b`.
- Nginx and PostgreSQL are active with `NRestarts=0`. Odoo is active with
  `NRestarts=2`: the passlib `ModuleNotFoundError` caused its automatic exit at
  10:24:43, and systemd restarted it at 10:24:53. This preceded the 10:40
  deployment and the successful live run at 10:47. This batch issued no
  service-control command, but no claim is made that the whole observation
  window had no restart.

## Historical 218-ID account-return and journal-analysis batch

- Added `account.return.search`, `account.return.get`,
  `account.return.summary`, `account.return.type.list`,
  `account.return.check.list`, `account.return.check.get`,
  `journal.accounting_date.resolve`, and `journal_item.analysis.summary`
  through fixed public contracts, CLI dispatch, bridge actions, and native Odoo
  runtime routes.
- At that checkpoint the registry had 218 IDs and 197 handlers (126 reads and 71 writes).
  Live success evidence covered 125 reads and 70 writes. There were 21 disabled
  descriptors, 399 schema documents, and statuses of 184 `unconfigured`, 13
  `degraded`, and 21 `disabled`.
- The focused local feature selection passed 84 cases and the registry/schema
  selection passed 29. The synchronized server passed 48 core feature tests;
  no result is claimed for the broader server run whose SSH output stream closed
  before its exit status was available.
- One shared rollback-only live smoke ran all eight reads in both
  `odoo_cli_v4_dev` and `odoo_cli_v4_e2e` as uid 5/company 1 and recorded
  `2 passed in 5.09s`. Fresh-cursor verification plus an independent SQL query
  found zero return/check marker residue in both databases.
- Capability-ID-list SHA-256:
  `c96f9c1510501eaa3ca09b62a697b6577ddaa79688a9b30c563f704926c8cef3`.
  Registry-file SHA-256:
  `e9180a7f420030debed3f535d920f1deae7e61b5a8f0d417af1a1720c686a4f3`.
  Canonical registry digest:
  `274533a2aaf573b32029b507e21db7864fbd2ac2587637a14e344cfcffb299ec`.
- All 37 synchronized files are byte-identical. The synchronization archive is
  149640 bytes with SHA-256
  `f28becccf575a506d41fbed487f796e39e76e68ebd15c4d59580836556ba3140`.
  The retained live log has SHA-256
  `6e9d2ac69f14bfc2c9b00ba8609493cba67870f7173df29807d73eb5e09f033a`.
  Odoo, Nginx, and PostgreSQL remained active with unchanged start timestamps
  and restart counts.

## Previous management-reporting and period-context batch

- Added `report.customer_statement`, `report.followup`,
  `invoice.analysis.search`, `invoice.analysis.summary`,
  `company.lock_dates.inspect`, `company.fiscal_year.resolve`,
  `fiscal_year.search`, and `fiscal_year.get` through fixed public contracts,
  CLI dispatch, bridge actions, and Odoo runtime routes.
- The current registry has 210 IDs and 189 handlers (118 reads and 71 writes).
  Live success evidence covers 117 reads and 70 writes; the two remaining
  implemented paths are the known positive-fixture/server-defect exceptions.
  There are 21 disabled descriptors and 383 schema documents.
- Focused capability/bridge/runtime tests passed 131 cases. The unified new CLI
  selection passed 6 cases, the two report CLI cases passed 2, and the registry
  selection passed 17. Final count-related selections passed 14 cases locally
  and 43 cases after synchronization on the server.
- One shared rollback-only live smoke ran all eight reads in both
  `odoo_cli_v4_dev` and `odoo_cli_v4_e2e` as uid 5/company 1 and recorded
  `2 passed in 6.62s`. A transaction-local fiscal-year fixture was absent when
  checked through a fresh cursor after rollback.
- Capability-ID-list SHA-256:
  `6ba5e3d877b6fa857d1689c14da30132b63572c801e2d25b4af4e30c10a6dd82`.
  Registry-file SHA-256:
  `58de9401661a4ebeefc7892c3a4da20d38bda1fe34d1502b9588d957ee936d94`.
  Canonical registry digest:
  `50451ae4a3b6d145ad4d89c3b3f6f7a11b70472a6f35099b609842d19fc3e19f`.
- The explicit 41-file source archive is 156351 bytes with SHA-256
  `37258845be076e3c6432d6291ed6fc690578b2befd8e3674a9e848d5f01d0191`;
  all entries are unique, relative, and present in the active tree.

## Previous 262-ID fiscal-position, journal-group, and localization-readiness batch

- Added seven writes: fiscal-position create/update/account-mapping replacement/
  archive/restore and journal-group create/update. Enabled two previously
  disabled reads: China and Singapore localization-readiness inspection.
- The final registry has 262 unique IDs, 245 handlers (146 reads and 99 writes),
  17 disabled descriptors, 495 schemas, and statuses of 226 `unconfigured`, 19
  `degraded`, and 17 `disabled`.
- A broader focused selection passed 111 tests locally and on the server. Live
  evidence then corrected the native fiscal-position sequence default to zero
  and verified HTML notes against Odoo's `html_sanitize`; the final current-code
  write regression passed 75 tests locally and on the server.
- One guarded shared live test passed in 6.18 seconds. In each of `v4-dev` and
  `v4-e2e`, all seven writes ran first execution plus immediate replay, both
  readiness reads succeeded, the transaction rolled back, and fresh-cursor
  checks found no record or temporary manager-group residue.
- Four complete public CLI reads then passed: China/company 1 and Singapore/
  company 2 in both aliases.
- Capability-ID-list SHA-256:
  `9212e9829355e5a3c44c1a0eb314e63a15240c84b5e310a540307f2e6b8a9ce8`.
  Registry-file SHA-256:
  `42d535a3520d1807cf9a22c843687573db056e81b42fc5a92f6e1966b3f50d3f`.
  Canonical registry digest:
  `64d0308eece99ad4f419642a447582d4d2e8408bd42c8c0500030f0657950dce`.
- Evidence directory:
  `/opt/odoo-accounting-cli-v4/.tooling/accounting-configuration-batch-150e0d50-7fa3-48fb-9ee3-a6123945260b`.
  Final live-log SHA-256:
  `be5cf6902f9fb5f96b73aa3597a59d9bb45d4e2f5cb1b3c94029a7877f7d9d4c`.
  Public-read-log SHA-256:
  `580d0a674a430a1144b71bda206bf05840280dd3e1d649fa9aae0debe5fb3083`.
- Odoo, Nginx, PostgreSQL, and Pi before/after snapshots are byte-identical,
  both with SHA-256
  `8b733a18fb71202470a3a387acdae26a11493f9f5e452d67110ed9da81845ecb`.
  No service-control command or business-database write was used.
- `fiscal_position.create` remains degraded only for concurrent exactly-once
  creation: Odoo has no database-unique request key. Fiscal-position tax mapping
  is deliberately separate because Odoo 19 stores source relations on shared
  destination-tax records.

## Current 289-ID sales-invoice and stock-transfer write batch

- Added `sale.order.invoice.create`, `stock.transfer.create`,
  `stock.transfer.confirm`, `stock.transfer.assign`,
  `stock.transfer.quantities.set`, `stock.transfer.validate`,
  `stock.transfer.unreserve`, and `stock.transfer.cancel` through the fixed
  core-write JSON contract and company/user-scoped native Odoo runtime.
- The registry has 289 unique IDs, 272 handlers (165 reads and 107 writes),
  549 schemas, and statuses of 251 `unconfigured`, 21 `degraded`, and 17
  `disabled`. `sale.order.invoice.create` and `stock.transfer.create` are
  degraded only because ordinary replay is implemented but concurrent
  exactly-once creation is not database-enforced.
- The final server runtime regression passed `73 tests in 0.52s`. The shared
  live smoke passed `2 tests in 13.78s`: both isolated databases completed all
  eight first executions and immediate replays as uid 5/company 1, created one
  real partial-transfer backorder, and then proved transaction, record, and
  temporary-group rollback.
- Live testing exposed and fixed one production Odoo 19 incompatibility:
  `uom.uom.category_id` no longer exists, so V4 now uses Odoo 19's
  `_has_common_reference()` method to retain the same-dimension safety check.
  Fixture-only corrections supplied a valid product income account, accepted
  the normal null name of a draft invoice, and avoided the reserved `ODACV4`
  namespace in caller-provided stock origins.
- The server public `capabilities list` returned 289 IDs with canonical digest
  `72b2a90c0e8da798156856ccb9285a80ce4b1572e108147ff81feb8b8c21eb71`;
  the registry-file SHA-256 is
  `f2e8f929ff607ceb047cf9ef2a53de306530bee2ced272fba9f04a9dcd55239f`.
- Evidence directory:
  `/opt/odoo-accounting-cli-v4/.tooling/stock-transfer-write-batch-22d9cdc2-0c95-489f-aeeb-8bf41eb2516e`.
  Final live-log SHA-256:
  `d22c43c5d1ee330dcf7bdccf9928dd87bc6bbdf16feb8023eee35d5bc3207794`;
  final runtime-unit log SHA-256:
  `32a4c93fb4cc386e01085534b38275d22eb1543759d56b797e85286ad4c12a45`.
  The final three-file compatibility patch and its pre-overwrite backup have
  SHA-256 values
  `a8bbeb1e2e703a6e5d1e12c337b202b79b4e1ae63e7b89e34d276ce91893432d`
  and
  `e430f658707d0b0e47142cced269e8c005d2ff63efbdb1bc748b8262639ac583`.
- Independent read-only SQL checks returned zero residue for every retained
  failed-attempt marker in both isolated databases. Odoo, Nginx, and PostgreSQL
  retained their pre-run start timestamps and restart counts; no service was
  restarted.

## Previous 281-ID specialist/localized report-export extension

- Added nine read-only exports: journal, asset, deferred expense, deferred
  revenue, multicurrency revaluation, China balance sheet/profit and loss/cash
  flow, and Singapore GST. They reuse the fixed report-export contract and
  bridge; no arbitrary report, model, or method dispatcher was added.
- Journal export uses Odoo's official `dispatch_report_action` because its
  report handler overrides PDF/XLSX behavior. The other eight use the official
  report export methods directly. China and Singapore exports fail closed on
  the wrong fiscal-country/chart context.
- The registry has 281 unique IDs, 264 handlers (165 reads and 99 writes), 17
  disabled descriptors, 533 schemas, and statuses of 245 `unconfigured`, 19
  `degraded`, and 17 `disabled`.
- Focused local verification passed 219 tests with one opt-in live test skipped;
  targeted Ruff passed. The synchronized server selection passed 219 tests in
  547.23 seconds.
- A post-review journal-metadata regression passed 117 tests locally in 481.11
  seconds and 72 tests on the synchronized server in 6.16 seconds.
- One shared read-only smoke passed in 63.06 seconds and executed all 19 fixed
  report exports in PDF and XLSX in both isolated aliases: 76 native files in
  total. Public CLI checks also passed for a 47,484-byte journal PDF in
  `v4-dev`/company 1 and a 7,133-byte Singapore GST XLSX in
  `v4-e2e`/company 2; Base64, length, SHA-256, binding, and file magic were
  independently verified.
- Capability-ID SHA-256:
  `04f3e17865e63eeb5a3765a8f7de6f7c93e755d6687e5b3dcffcd33f912f29da`.
  Canonical registry digest:
  `a0d195d91a32dfd012f4b76909d51e0302339399ffe3802bf00f6f799f99f9a7`.
  Deployed registry-file SHA-256:
  `6dbebf26b751b45790a4ac38646a46428e8e810c5e212bcc9ccb84fdae9805e3`.
- Evidence directory:
  `/opt/odoo-accounting-cli-v4/.tooling/financial-report-export-extension-838b729d-342c-49a2-86ff-04846b2ae30e`.
  Focused-log SHA-256:
  `e47f6da4082242d3edc6f4faa5745fedb483454792b1f7eb0282fe4163b202ce`.
  Live-log SHA-256:
  `76e920d6272f33039ccefd9cac89cbfaa0d5658c0b8b7115806ed43afdc89379`.
  Public-CLI summary SHA-256:
  `dd426f89da4344536a5357a5e700ac8770411dfb3848a3edd66250eb4f0938f8`.
- The final 30-file source archive SHA-256 is
  `8ea9a5a6cd28fa2ec43e45f7ec40e79fbae1a561e451b79bce60b59d0cd76394`;
  the validated pre-sync rollback archive SHA-256 is
  `aa6726e94967370fe31f17a8d5b9d76b7e8ddd7573ff45312efa5cf7dad7ec04`.
- Odoo, Nginx, PostgreSQL, and Pi before/after snapshots are byte-identical,
  both with SHA-256
  `ce322158b9bf7d81e844f198095ac920cff2436ca1436e3c01c9f15595634fdc`.
  No service-control command or business-database write was used.
- The first live attempt is retained but not counted: it ran as `root` and
  stopped at PostgreSQL peer authentication before reaching capability logic.
  The passing run used the Odoo service account. These checks prove valid files
  on the present fixtures, not golden accounting values for every dataset.

## Previous 272-ID native financial-report export batch

- Added ten read-only exports: trial balance, balance sheet, profit and loss,
  cash flow, tax, general ledger, partner ledger, aged receivable, aged payable,
  and executive summary. Each supports native Odoo PDF and XLSX output for
  posted entries through one fixed action; no generic dispatcher was added.
- The final registry has 272 unique IDs, 255 handlers (156 reads and 99 writes),
  17 disabled descriptors, 515 schemas, and statuses of 236 `unconfigured`, 19
  `degraded`, and 17 `disabled`.
- Server focused regression passed 269 tests in 498.97 seconds. The final bridge-
  client/runtime selection passed 47 tests in 0.23 seconds locally and on the
  synchronized server.
- One shared read-only smoke passed in 33.50 seconds and executed all ten
  capabilities in PDF and XLSX in both isolated aliases: 40 native exports in
  total as uid 5/company 1.
- Public CLI end-to-end checks passed for a 39,097-byte trial-balance PDF in
  `v4-dev` and a 7,482-byte general-ledger XLSX in `v4-e2e`; Base64, byte count,
  SHA-256, MIME/extension, and file magic were verified.
- Capability-ID-list SHA-256:
  `b14c19b3fcc05787deb9c924f200166a5ce7139d4648a404f697aabee493ba64`.
  Registry-file SHA-256:
  `51511d050cd6b28f037741f61107acab9734b618fc6029e82a55b185383b0d48`.
  Canonical registry digest:
  `2c3009784a48c56fe337cd3f09a634c2f4a411302575aac0b6a61930343654b2`.
- Evidence directory:
  `/opt/odoo-accounting-cli-v4/.tooling/financial-report-export-batch-2282bf69-1a4f-4356-a6b1-a19103d11f86`.
  Focused-log SHA-256:
  `de680148ba680ed4f8b74e3989bc10de2acc681210c0841e86ceb153fb46548b`.
  Live-smoke-log SHA-256:
  `63025c82e5e9b777024580d048eda07275dfe6b6ff776b46a7ac1fb8533f6c21`.
  Public-CLI verification-summary SHA-256:
  `920f738f6cd635d49785f8c6fbd1f5e78788d12de72493d54531558a421a2c08`.
- Odoo, Nginx, PostgreSQL, and Pi before/after snapshots are byte-identical,
  both with SHA-256
  `baa61f348401615814ebee1a2f59071036d5be8ae79765febcb87fa5217de105`.
  No service-control command or business-database write was used.
- Two readable pre-overwrite rollback archives are retained: seven prior shared
  files in `pre-sync-backup.tar.gz` (SHA-256
  `7b69df96e0bb7961b16d6306dba725c8e887ea1edd033041547a4e0d95262000`)
  and two prior incremental files in `pre-client-limit-backup.tar.gz` (SHA-256
  `936dbf8bce53914419377b7087a56de2904eba8ed21532d1ef22352bafee1279`).
  These are scoped file backups rather than a whole-server/database snapshot.

The coverage boundary is derived from installed modules and business workflows,
not from the number 281. Both isolated databases have the same 82 installed
modules. A read-only audit classified 39 accounting or adjacent modules into 25
primary-scope modules, five conditional EDI/compliance modules, and nine UI,
communication, or technical modules that do not require independent commands.
Final coverage requires a module/workflow/lifecycle matrix with no unexplained
in-scope gap.

## Remaining G5 work

- The preceding invoice/payment/reconciliation depth checkpoint is complete, but
  it does not prove those full workflows or the overall library complete. Keep
  the current 289-ID registry as an exact checkpoint rather than a target count;
  retain 281 as the historical specialist/localized report-export checkpoint,
  retain 272 as the historical native-report-export checkpoint, retain 262 as
  the historical fiscal-position/localization checkpoint,
  retain 236 as the historical order-document read checkpoint and 228 as the
  historical operational-inventory checkpoint.
- Re-audit the installed module/workflow/lifecycle matrix and select the next
  coherent 8-12-command ability gap. Do not infer a target from the command
  count or force disabled descriptors merely to consume them.
- Fix or upgrade the server `exchange_currency_rate` constraint, then rerun the
  existing `asset.validate` success path without weakening ACL, company scope,
  confirmation, native Odoo behavior, or rollback guarantees.
- Implement and verify the remaining planned master-data, journal, document,
  payment, reconciliation, localization, and diagnostic read/write capabilities.
- Continue adapting only verified remaining Odoo accounting reports through
  their real handlers; partner- or journal-parameterized reports require a
  separate frozen contract rather than a generic export dispatcher.
- Add runtime availability/doctor reporting for each database, company, user,
  module, configuration, and ACL combination.
- Meet the final coverage and Pi natural-language end-to-end gates in their
  later phases.

The static public registry remains environment-independent: real availability
is evaluated at runtime and is not globally asserted merely because these two
synthetic environments passed.
