# G1 bootstrap report

## Result

Passed. This gate establishes independent build and test tooling only; it does not provide accounting business capabilities or release readiness.

## Reproducible inputs

- CLI-Anything: tag `v0.4.0`, commit `dc7392489222dbcc520817609290755d6dd8b0bb`
- CLI-Anything source tree SHA-256: `b81f9254adf59e5cbd25457e6d55e05add73bfe05eae1171da5e5641a07f1bad`
- Installed extension tree SHA-256: `5d66e49bc5c0da438cb8c8008c71a1e1d4d53d9f8c8c0bf6a7bff9bf592d16b5`
- Python dependency lock SHA-256: `1445aa7afed7a159bc18e26182a187e76e2a2ceaae2bf68c6b00724e3ee65850`
- npm lock SHA-256: `f57cb96e6741bde85cc4057c41a3abf3368e1bf4d363d8d9a2b945fa3e54f35d`
- Python: 3.12.3; pip: 26.2.1; npm runner: 12.0.2

The source tree digest uses C-locale byte ordering, NUL-delimited paths, and a GNU `sha256sum` manifest.

## Verification

- Python unit tests passed.
- Official CLI-Anything extension tests passed: 14 of 14.
- A fresh Pi discovery process loaded exactly the five expected CLI-Anything commands with no loader errors.
- Hash-locked Python installation, `pip check`, package build, clean wheel installation, and bootstrap command smoke test passed.
- Public-tree scanner canaries and the real proposed Git tree passed, including restricted-source similarity checks.

## Protection result

- No existing database was created, altered, or removed.
- Existing Odoo, PostgreSQL, reverse-proxy, and Pi bridge services were not restarted or reloaded.
- Existing Odoo source, V2/V3 installations, legacy harness, and Pi sessions remain outside the V4 write scope.
- No Odoo/custom source snapshot, raw evidence, connection material, credential, or private server path is included in this public tree.

## Recorded deviations

- The host npm 10.9.8 runner failed internally during dependency resolution. G1 uses the isolated, exactly pinned npm 12.0.2 runner instead.
- An apparent extension tree digest mismatch was traced to locale-dependent file ordering; individual file hashes were unchanged. The canonical digest algorithm now fixes `LC_ALL=C`.

## Next gate

G2 will create the provenance-checked accounting source snapshot and the two dedicated isolated test databases. No existing database is a test target.
